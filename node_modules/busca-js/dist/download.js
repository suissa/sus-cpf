'use strict';

var fetch = require('node-fetch');

exports.search = function (query) {
  var url = 'http://mdn.io/' + encodeURIComponent(query);
  console.log('url', url)
  return fetch(url).then(function (res) {
    // console.log('res', res)
    return res.text();
  });
};